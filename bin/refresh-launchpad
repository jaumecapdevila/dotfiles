#!/usr/bin/env bash

set -euo pipefail

source "$DOTFILES_PATH/scripts/core/docs.sh"

##? Refresh Launchpad Contents in MacOS X
#?? 1.0.0
##?
##? Usage:
##?    refresh-launchpad

docs::eval "$@"

rm ~/Library/Application\ Support/Dock/*.db ; killall Dock
