#!/usr/bin/env bash

set -euo pipefail

source "$DOTFILES_PATH/scripts/core/docs.sh"

##? Kill process named
#?? 1.0.0
##?
##? Usage:
##?    kill_named

docs::eval "$@"

ps aux |
  fzf --height 50% --ansi |
  awk '{print $2}'|
  xargs kill -9

echo 'done!'
