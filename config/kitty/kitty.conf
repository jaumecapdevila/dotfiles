# UI config
font_family Operator Mono Book
bold_font Operator Mono Bold
italic_font Operator Mono Book Italic
bold_italic_font Operator Mono Bold Italic
font_size 20.0
# background_opacity 0.5

adjust_line_height 120%
cursor_blink_interval 0
disable_ligatures cursor
hide_window_decorations titlebar-only

window_border_width 0
window_margin_width 0
window_padding_width 4
remember_window_size  yes
confirm_os_window_close 0

# macOS config
macos_thicken_font 0.75
macos_show_window_title_in none
macos_option_as_alt left
macos_custom_beam_cursor no

# Other config
scrollback_lines 10000
enable_audio_bell no
allow_remote_control no

# Theme config
tab_bar_min_tabs 2
tab_bar_edge top
tab_bar_style powerline
tab_powerline_style slanted
tab_title_template {title}{' :{}:'.format(num_windows) if num_windows > 1 else ''}

# Mappings

# https://sw.kovidgoyal.net/kitty/conf/#shortcut-kitty.Send-arbitrary-text-on-key-presses
# map ctrl+alt+a send_text all Special text
# the first argument is the keyboard modes in which to activate the shortcut
# the available values are normal, application, kitty or all

# Use ctrl+shift+arrows in tmux
map ctrl+shift+right    no_op
map ctrl+shift+left     no_op

# Custom tmux mappings
map cmd+t send_text all \x1Ac

# We use these in Neovim
map shift+enter send_text all \x1b[13;2u
map ctrl+enter send_text all \x1b[13;5u

# Jump to beginning and end of word.
map alt+left  send_text all \x1b\x62
map alt+right send_text all \x1b\x66

# Jump to beginning and end of line.
map cmd+left  send_text all \x05
map cmd+right send_text all \x05

# Ctrl and click to open a link
mouse_map ctrl+left press ungrabbed,grabbed mouse_click_url

# BEGIN_KITTY_THEME
include themes/frappe.conf
# END_KITTY_THEME
