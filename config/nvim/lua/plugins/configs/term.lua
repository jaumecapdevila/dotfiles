local options = {
  open_mapping = [[<c-t>]],
  hide_numbers = true,
  shade_terminals = false,
  direction = "float",
  close_on_exit = true, -- close the terminal window when the process exits
}

return options
